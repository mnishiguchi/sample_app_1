- provide(:title, "Sign up")

%h1
  Sign up
.row
  .col-md-6.col-md-offset-3
    = form_for(@user) do |f|

      = render 'users/fields', f: f

      = f.button "Create my account", class: "btn btn-success",
        data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> Processing..." }
