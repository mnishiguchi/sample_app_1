= provide(:title, "All users")

/ All users
%h1
  All users
%p
  Download:
  = link_to "CSV", users_path(format: "csv")
  = "|"
  = link_to "Excel", "#"

= render 'search_box'

= will_paginate @users
%ul.users
  = render @users
= will_paginate @users
