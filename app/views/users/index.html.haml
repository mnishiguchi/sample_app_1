= provide(:title, "All users")

/ All users
%h1
  All users

%p.pull-right
  Download:
  = link_to "CSV", users_path(format: "csv")
  = "|"
  = link_to "Excel", "#"

%section
  = render 'search_box'

= will_paginate @users
%ul.users
  = render @users
= will_paginate @users
