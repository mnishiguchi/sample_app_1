-# Rails infers that @feed_items is a collection of microposts based on its class.

- if @feed_items.any?
  %ol.microposts
    = render @feed_items
  = will_paginate @feed_items
- else
  Currently none
